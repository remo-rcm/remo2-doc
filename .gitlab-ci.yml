image: alpine

variables:
  GIT_SSL_NO_VERIFY: "1"

pages:
  script:
  - conda env create -f environment.yml
  - make html
  - mv build/html/ public/
  artifacts:
    paths:
    - public
  only:
  - master
